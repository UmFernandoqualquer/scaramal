# Gerenciador de Usuários

Este projeto é um site simples em PHP que permite inserir, listar e excluir usuários usando comandos SQL (`INSERT`, `SELECT`, `DELETE`, `ALTER`).

## Requisitos

- PHP 7+
- MySQL
- Servidor local (XAMPP, WAMP, etc.)

## Instalação

1. Clone o repositório:
